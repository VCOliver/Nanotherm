{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"Bem-vindo \u00e0 Documenta\u00e7\u00e3o do Nanotherm","text":"<p>O Nanotherm \u00e9 uma plataforma moderna para abla\u00e7\u00e3o por radiofrequ\u00eancia (RFA) desenvolvida pela Metala Nanofluidos, baseada no projeto SOFIA. Esta documenta\u00e7\u00e3o detalha como instalar, configurar e usar o sistema.</p>"},{"location":"#instalacao","title":"Instala\u00e7\u00e3o","text":"<p>O Nanotherm requer Python 3.12 ou superior. Recomendamos usar o Conda para gerenciar o ambiente:</p> <pre><code># Clone o reposit\u00f3rio\ngit clone https://github.com/VCOliver/Nanotherm\ncd Nanotherm\n\n# Crie e ative o ambiente conda\nconda env create -f environment.yml\nconda activate nanotherm-venv\n\n# Instale o pacote em modo de desenvolvimento\npoetry install\n</code></pre>"},{"location":"#configuracao","title":"Configura\u00e7\u00e3o","text":"<p>O sistema usa um arquivo TOML para configura\u00e7\u00e3o. Um exemplo b\u00e1sico:</p> <pre><code>[logging]\nlevel = \"DEBUG\"  # Ou \"deploy\" para produ\u00e7\u00e3o\nformat = \"%(asctime)s [%(levelname)s] %(name)s: %(message)s\"\ndate_format = \"%d-%m-%Y %H:%M:%S\"\n\n[pid]\nkp = 1.0\nki = 0.2\nkd = 0.05\n</code></pre>"},{"location":"#uso-basico","title":"Uso B\u00e1sico","text":"<p>Para executar o Nanotherm:</p> <pre><code># Via poetry\npoetry run nanotherm\n\n# Ou diretamente ap\u00f3s instala\u00e7\u00e3o\npython -m nanotherm\n</code></pre>"},{"location":"#estrutura-do-projeto","title":"Estrutura do Projeto","text":"<pre><code> src/nanotherm/           # C\u00f3digo fonte principal\n    \u251c\u2500\u2500 app/                # Aplica\u00e7\u00e3o principal e entry points\n    \u251c\u2500\u2500 core/              # Core business logic\n    \u2502   \u251c\u2500\u2500 domain/       # Domain models and value objects\n    \u2502   \u2514\u2500\u2500 entities/     # Core business interfaces\n    \u251c\u2500\u2500 infrastructure/   # Concrete implementations\n    \u2502   \u2514\u2500\u2500 controllers/  # Controller implementations\n    \u251c\u2500\u2500 services/        # Application services\n    \u2514\u2500\u2500 config/         # Configuration management\n</code></pre> <p>O projeto segue uma arquitetura limpa (Clean Architecture) com separa\u00e7\u00e3o clara entre: - Domain: Regras de neg\u00f3cio e interfaces core - Infrastructure: Implementa\u00e7\u00f5es concretas - Services: Orquestra\u00e7\u00e3o de componentes</p>"},{"location":"#licenca","title":"Licen\u00e7a","text":"<p>O Nanotherm \u00e9 licenciado sob Apache License 2.0. Veja o arquivo LICENSE para mais detalhes.</p>"},{"location":"#links-uteis","title":"Links \u00dateis","text":"<ul> <li>C\u00f3digo fonte</li> <li>Documenta\u00e7\u00e3o da API</li> <li>Sobre o projeto</li> </ul>"},{"location":"#suporte","title":"Suporte","text":"<p>Para suporte ou d\u00favidas, entre em contato: - Email: metala.nanofluidos@gmail.com - Site: metalanano.com</p>"},{"location":"about/","title":"Sobre a Metala","text":"<p>A Metala Nanofluidos \u00e9 uma startup de base tecnol\u00f3gica sediada em Bras\u00edlia, dedicada \u00e0 inova\u00e7\u00e3o em sa\u00fade por meio da nanotecnologia. Nosso foco est\u00e1 no desenvolvimento de solu\u00e7\u00f5es que aumentem a efic\u00e1cia de tratamentos oncol\u00f3gicos, com \u00eanfase em terapias menos invasivas e mais acess\u00edveis. Nosso principal foco est\u00e1 no desenvolvimento de nanocomp\u00f3sitos inovadores, como o GPA (Grafeno\u2011Polietilenoimina\u2011Ouro).</p> <p>Nosso trabalho nasce da pesquisa acad\u00eamica e \u00e9 impulsionado por uma equipe multidisciplinar com experi\u00eancia em nanomedicina, engenharia biom\u00e9dica e f\u00edsica aplicada.</p> <p>Para saber mais sobre nossa vis\u00e3o e projetos, visite nosso site: metalanano.com</p>"},{"location":"about/#o-projeto-nanotherm","title":"O Projeto Nanotherm","text":"<p>O Nanotherm \u00e9 nossa principal iniciativa para aprimorar a tecnologia de Abla\u00e7\u00e3o por Radiofrequ\u00eancia (RFA), um procedimento minimamente invasivo utilizado no tratamento de certos tipos de tumores s\u00f3lidos.</p>"},{"location":"about/#o-desafio","title":"O Desafio","text":"<p>A efic\u00e1cia da RFA convencional pode ser limitada em tumores maiores, especialmente devido ao fen\u00f4meno de roll-off \u2014 a carboniza\u00e7\u00e3o precoce do tecido ao redor do eletrodo, que impede a propaga\u00e7\u00e3o cont\u00ednua da energia t\u00e9rmica.</p>"},{"location":"about/#nossa-solucao","title":"Nossa Solu\u00e7\u00e3o","text":"<p>O Nanotherm busca superar essa limita\u00e7\u00e3o com uma abordagem integrada entre materiais avan\u00e7ados e controle t\u00e9rmico. Estamos desenvolvendo uma plataforma que combina:</p> <ul> <li>Formula\u00e7\u00f5es de nanofluidos com propriedades otimizadas para condu\u00e7\u00e3o de calor;  </li> <li>Estrat\u00e9gias de controle de energia, como pulsos modulados;  </li> <li>Eletrodos especiais com melhor distribui\u00e7\u00e3o t\u00e9rmica.</li> </ul> <p>Essas inova\u00e7\u00f5es visam melhorar a homogeneidade e o alcance da necrose t\u00e9rmica, ampliando o potencial da RFA em tumores de maior volume, com foco na seguran\u00e7a e efic\u00e1cia cl\u00ednica.</p>"},{"location":"about/#oportunidade-de-mercado","title":"Oportunidade de Mercado","text":"<p>O mercado de oncologia \u00e9 um dos setores da sa\u00fade com maior crescimento global, impulsionado pelo aumento da incid\u00eancia de c\u00e2ncer e pela demanda por tratamentos mais eficazes e acess\u00edveis. Nesse contexto, a Metala Nanofluidos se posiciona ao unir nanotecnologia e abla\u00e7\u00e3o por radiofrequ\u00eancia para oferecer solu\u00e7\u00f5es promissoras.</p>"},{"location":"about/#cenario-brasileiro","title":"Cen\u00e1rio Brasileiro","text":"<p>Dados do Instituto Nacional de C\u00e2ncer (INCA) estimam que o Brasil registrou mais de 480 mil novos casos de c\u00e2ncer em 2023. Aproximadamente 20% desses pacientes podem ser eleg\u00edveis para tratamentos de RFA, o que corresponde a cerca de 96 mil pacientes anuais.</p> <p>Com um custo m\u00e9dio de R$\u202f30\u202f000 por sess\u00e3o de RFA, o mercado potencial no Brasil ultrapassa R$\u202f2,8\u202fbilh\u00f5es por ano. Al\u00e9m disso, o setor privado de sa\u00fade tem demonstrado interesse em tecnologias inovadoras, como demonstrado por investimentos recentes em pesquisa cl\u00ednica e dispositivos m\u00e9dicos.</p>"},{"location":"about/#diferenciais-competitivos","title":"Diferenciais Competitivos","text":"<p>O nanocomp\u00f3sito GPA da Metala apresenta caracter\u00edsticas que podem se traduzir em vantagens competitivas para centros de tratamento:</p> <ul> <li>Maior precis\u00e3o t\u00e9rmica: sua formula\u00e7\u00e3o pode melhorar a uniformidade de aquecimento, reduzindo \u00e1reas frias e pontos de carboniza\u00e7\u00e3o;</li> <li>Potencial redu\u00e7\u00e3o de sess\u00f5es: ao aumentar a taxa de abla\u00e7\u00e3o por sess\u00e3o, h\u00e1 possibilidade de diminuir o n\u00famero total de procedimentos;</li> <li>Integra\u00e7\u00e3o ao SUS e redes privadas: alinhado \u00e0s diretrizes de inova\u00e7\u00f5es terap\u00eauticas, o GPA pode ser incorporado tanto em servi\u00e7os p\u00fablicos quanto em cl\u00ednicas particulares.</li> </ul>"},{"location":"about/#perspectivas-futuras","title":"Perspectivas Futuras","text":"<p>Com parcerias em desenvolvimento e estudos pr\u00e9-cl\u00ednicos em andamento, a Metala Nanofluidos est\u00e1 bem posicionada para ampliar sua participa\u00e7\u00e3o no mercado de RFA. A ado\u00e7\u00e3o por centros de pesquisa e hospitais de refer\u00eancia poder\u00e1 acelerar o processo de aprova\u00e7\u00e3o regulat\u00f3ria e consolidar o uso do GPA como uma ferramenta de alta performance em abla\u00e7\u00e3o t\u00e9rmica.</p>"},{"location":"about/#navegue-pela-documentacao","title":"Navegue pela Documenta\u00e7\u00e3o","text":"<p>Use o menu ao lado para explorar os detalhes t\u00e9cnicos, guias de instala\u00e7\u00e3o e refer\u00eancias da API do Nanotherm.</p>"},{"location":"reference/app/","title":"Application Reference","text":"<p>This page documents the core application components of Nanotherm.</p>"},{"location":"reference/app/#overview","title":"Overview","text":"<p>The <code>app</code> module contains the main application class that orchestrates: - Component lifecycle management - Control loop execution - Error handling and shutdown procedures</p>"},{"location":"reference/app/#module-documentation","title":"Module Documentation","text":"<p>Imports: - PIDParams - Discrete Transfer Function - PID Controller - Simulation Gateway - Control Loop</p>"},{"location":"reference/app/#nanotherm.app.app","title":"app","text":"<p>Main application module for the nanotherm system.</p> <p>This module contains the core App class that handles the main application logic and lifecycle management.</p>"},{"location":"reference/app/#nanotherm.app.app.App","title":"App","text":"<p>Main application class for nanotherm.</p> <p>Handles initialization and execution of the core application logic.</p> <p>Attributes:</p> Name Type Description <code>config</code> <code>Dict[str, Any]</code> <p>Configuration dictionary containing application settings.</p> Source code in <code>src/nanotherm/app/app.py</code> <pre><code>class App:\n    \"\"\"\n    Main application class for nanotherm.\n\n    Handles initialization and execution of the core application logic.\n\n    Attributes:\n        config (Dict[str, Any]): Configuration dictionary containing application settings.\n    \"\"\"\n\n    def __init__(self, config: Dict[str, Any]) -&gt; None:\n        \"\"\"\n        Initialize the application with the given configuration.\n\n        Args:\n            config (Dict[str, Any]): Dictionary containing application configuration settings.\n        \"\"\"\n        self.config = config\n        log.info('Starting main application.')\n\n    def run(self) -&gt; None:\n        \"\"\"\n        Execute the main application logic.\n\n        This method starts the main processing loop and handles the core\n        application functionality, including initializing the control system,\n        running the simulation, and saving results.\n        \"\"\"\n        gains = list(self.config['pid'].values())\n        pid_params = PIDParams(*gains)\n        log.debug(f'PID gains set to {pid_params}')\n        Ts = self.config['control_system']['sampling_time']\n\n        plant_tf = self.config['liver_tf']\n        plant = DiscreteTransferFunction(plant_tf['num'], plant_tf['den'], Ts)\n\n        setpoint = self.config['control_system']['setpoint']\n        controller = PIDController(pid_params, plant, setpoint=setpoint, Ts=Ts)\n\n        platform = Platform.get_hardware_type()\n        if platform == HardwareType.DESKTOP:\n            hal = CSV_HALGateway('data/input/combined.csv')\n        else:\n            log.warning('HAL for Raspberry Pi not yet implemented!')\n            raise HardwareError('Raspberry Pi not supported yet. Please use a desktop platform.')\n\n        # Example: run control loop for a few iterations (or implement a stop condition)\n        loop = ControlLoop(controller, sample_time=0.01, gateway=hal, simulating=True)\n        log.info(\"Starting control loop simulation...\")\n        loop.start()\n\n        try:\n            # Wait for the control loop to complete\n            loop.wait_for_completion()\n            log.info(\"Control loop simulation completed successfully\")\n\n        except KeyboardInterrupt:\n            log.info(\"Simulation interrupted by user\")\n            loop.stop()\n\n        except Exception as e:\n            log.error(f\"Error during simulation: {e}\")\n            loop.stop()\n            raise\n\n        finally:\n            # Ensure the loop is stopped\n            if loop.thread_is_alive():\n                loop.stop()\n\n        try:   \n            simulation_data = loop.get_simulation_data()\n            csv_path = self.save_simulation_results(simulation_data)\n\n        except Exception as e:\n            log.error(f\"Failed to save results: {e}\")\n\n\n    def save_simulation_results(\n        self, \n        simulation_data: List[Dict[str, Any]], \n        output_dir: str = 'data/output'\n    ) -&gt; str:\n        \"\"\"\n        Save simulation results to a CSV file using pandas.\n\n        Args:\n            simulation_data (List[Dict[str, Any]]): List of dictionaries containing simulation data.\n            output_dir (str, optional): Directory to save the output files. Defaults to 'data/output'.\n\n        Returns:\n            str: Path to the saved CSV file.\n\n        Raises:\n            Exception: If saving fails for any reason.\n        \"\"\"\n        try:\n            # Create output directory if it doesn't exist\n            output_path = Path(output_dir)\n            output_path.mkdir(parents=True, exist_ok=True)\n\n            # Generate filename with timestamp\n            timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n            csv_file = output_path / f\"control_loop_results_{timestamp}.csv\"\n\n            if simulation_data:\n                # Create DataFrame from simulation data\n                df = pd.DataFrame(simulation_data)\n\n                # Calculate basic statistics\n                stats_info = {\n                    'total_iterations': len(df),\n                    'simulation_duration': df['timestamp'].max() if not df.empty else 0,\n                    'final_measurement': df['measurement'].iloc[-1] if not df.empty else 0,\n                    'avg_output': df['measurement'].mean() if not df.empty else 0,\n                    'std_output': df['measurement'].std() if not df.empty else 0,\n                    'max_output': df['measurement'].max() if not df.empty else 0,\n                    'min_output': df['measurement'].min() if not df.empty else 0,\n                }\n\n                # Optionally, rename columns for CSV output only\n                df_out = df.rename(columns={\n                    'timestamp': 'timestamp (s)',\n                    'resistence': 'resistance (Ohm)',\n                    'control_action': 'control_action (V)',\n                    'setpoint': 'setpoint (W)',\n                    'measurement': 'measurement (W)',                    \n                })\n\n                # Save to CSV with proper formatting\n                df_out.to_csv(csv_file, index=False, float_format='%.6f')\n\n\n                log.info(f\"Results saved to CSV: {csv_file}\")\n                log.debug(f\"Simulation Statistics:\")\n                log.debug(f\"  - Total iterations: {stats_info['total_iterations']}\")\n                log.debug(f\"  - Duration: {stats_info['simulation_duration']} seconds\")\n                log.debug(f\"  - Final output: {stats_info['final_measurement']:.6f}\")\n                log.debug(f\"  - Average output: {stats_info['avg_output']:.6f}\")\n                log.debug(f\"  - STD output: {stats_info['std_output']:.6f}\")\n                log.debug(f\"  - Max output: {stats_info['max_output']:.6f}\")\n                log.debug(f\"  - Min output: {stats_info['min_output']:.6f}\")\n\n            else:\n                # Create empty DataFrame with correct columns\n                columns = ['timestamp', 'measurement', 'setpoint', 'control_action']\n                empty_df = pd.DataFrame(columns=columns)\n                empty_df.to_csv(csv_file, index=False)\n\n                log.warning(\"No simulation data collected - saved empty files with headers\")\n\n            return str(csv_file)\n\n        except Exception as e:\n            log.error(f\"Failed to save simulation results: {e}\")\n            raise\n</code></pre>"},{"location":"reference/app/#nanotherm.app.app.App.__init__","title":"__init__","text":"<pre><code>__init__(config: Dict[str, Any]) -&gt; None\n</code></pre> <p>Initialize the application with the given configuration.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>Dict[str, Any]</code> <p>Dictionary containing application configuration settings.</p> required Source code in <code>src/nanotherm/app/app.py</code> <pre><code>def __init__(self, config: Dict[str, Any]) -&gt; None:\n    \"\"\"\n    Initialize the application with the given configuration.\n\n    Args:\n        config (Dict[str, Any]): Dictionary containing application configuration settings.\n    \"\"\"\n    self.config = config\n    log.info('Starting main application.')\n</code></pre>"},{"location":"reference/app/#nanotherm.app.app.App.run","title":"run","text":"<pre><code>run() -&gt; None\n</code></pre> <p>Execute the main application logic.</p> <p>This method starts the main processing loop and handles the core application functionality, including initializing the control system, running the simulation, and saving results.</p> Source code in <code>src/nanotherm/app/app.py</code> <pre><code>def run(self) -&gt; None:\n    \"\"\"\n    Execute the main application logic.\n\n    This method starts the main processing loop and handles the core\n    application functionality, including initializing the control system,\n    running the simulation, and saving results.\n    \"\"\"\n    gains = list(self.config['pid'].values())\n    pid_params = PIDParams(*gains)\n    log.debug(f'PID gains set to {pid_params}')\n    Ts = self.config['control_system']['sampling_time']\n\n    plant_tf = self.config['liver_tf']\n    plant = DiscreteTransferFunction(plant_tf['num'], plant_tf['den'], Ts)\n\n    setpoint = self.config['control_system']['setpoint']\n    controller = PIDController(pid_params, plant, setpoint=setpoint, Ts=Ts)\n\n    platform = Platform.get_hardware_type()\n    if platform == HardwareType.DESKTOP:\n        hal = CSV_HALGateway('data/input/combined.csv')\n    else:\n        log.warning('HAL for Raspberry Pi not yet implemented!')\n        raise HardwareError('Raspberry Pi not supported yet. Please use a desktop platform.')\n\n    # Example: run control loop for a few iterations (or implement a stop condition)\n    loop = ControlLoop(controller, sample_time=0.01, gateway=hal, simulating=True)\n    log.info(\"Starting control loop simulation...\")\n    loop.start()\n\n    try:\n        # Wait for the control loop to complete\n        loop.wait_for_completion()\n        log.info(\"Control loop simulation completed successfully\")\n\n    except KeyboardInterrupt:\n        log.info(\"Simulation interrupted by user\")\n        loop.stop()\n\n    except Exception as e:\n        log.error(f\"Error during simulation: {e}\")\n        loop.stop()\n        raise\n\n    finally:\n        # Ensure the loop is stopped\n        if loop.thread_is_alive():\n            loop.stop()\n\n    try:   \n        simulation_data = loop.get_simulation_data()\n        csv_path = self.save_simulation_results(simulation_data)\n\n    except Exception as e:\n        log.error(f\"Failed to save results: {e}\")\n</code></pre>"},{"location":"reference/app/#nanotherm.app.app.App.save_simulation_results","title":"save_simulation_results","text":"<pre><code>save_simulation_results(simulation_data: List[Dict[str, Any]], output_dir: str = 'data/output') -&gt; str\n</code></pre> <p>Save simulation results to a CSV file using pandas.</p> <p>Parameters:</p> Name Type Description Default <code>simulation_data</code> <code>List[Dict[str, Any]]</code> <p>List of dictionaries containing simulation data.</p> required <code>output_dir</code> <code>str</code> <p>Directory to save the output files. Defaults to 'data/output'.</p> <code>'data/output'</code> <p>Returns:</p> Name Type Description <code>str</code> <code>str</code> <p>Path to the saved CSV file.</p> <p>Raises:</p> Type Description <code>Exception</code> <p>If saving fails for any reason.</p> Source code in <code>src/nanotherm/app/app.py</code> <pre><code>def save_simulation_results(\n    self, \n    simulation_data: List[Dict[str, Any]], \n    output_dir: str = 'data/output'\n) -&gt; str:\n    \"\"\"\n    Save simulation results to a CSV file using pandas.\n\n    Args:\n        simulation_data (List[Dict[str, Any]]): List of dictionaries containing simulation data.\n        output_dir (str, optional): Directory to save the output files. Defaults to 'data/output'.\n\n    Returns:\n        str: Path to the saved CSV file.\n\n    Raises:\n        Exception: If saving fails for any reason.\n    \"\"\"\n    try:\n        # Create output directory if it doesn't exist\n        output_path = Path(output_dir)\n        output_path.mkdir(parents=True, exist_ok=True)\n\n        # Generate filename with timestamp\n        timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n        csv_file = output_path / f\"control_loop_results_{timestamp}.csv\"\n\n        if simulation_data:\n            # Create DataFrame from simulation data\n            df = pd.DataFrame(simulation_data)\n\n            # Calculate basic statistics\n            stats_info = {\n                'total_iterations': len(df),\n                'simulation_duration': df['timestamp'].max() if not df.empty else 0,\n                'final_measurement': df['measurement'].iloc[-1] if not df.empty else 0,\n                'avg_output': df['measurement'].mean() if not df.empty else 0,\n                'std_output': df['measurement'].std() if not df.empty else 0,\n                'max_output': df['measurement'].max() if not df.empty else 0,\n                'min_output': df['measurement'].min() if not df.empty else 0,\n            }\n\n            # Optionally, rename columns for CSV output only\n            df_out = df.rename(columns={\n                'timestamp': 'timestamp (s)',\n                'resistence': 'resistance (Ohm)',\n                'control_action': 'control_action (V)',\n                'setpoint': 'setpoint (W)',\n                'measurement': 'measurement (W)',                    \n            })\n\n            # Save to CSV with proper formatting\n            df_out.to_csv(csv_file, index=False, float_format='%.6f')\n\n\n            log.info(f\"Results saved to CSV: {csv_file}\")\n            log.debug(f\"Simulation Statistics:\")\n            log.debug(f\"  - Total iterations: {stats_info['total_iterations']}\")\n            log.debug(f\"  - Duration: {stats_info['simulation_duration']} seconds\")\n            log.debug(f\"  - Final output: {stats_info['final_measurement']:.6f}\")\n            log.debug(f\"  - Average output: {stats_info['avg_output']:.6f}\")\n            log.debug(f\"  - STD output: {stats_info['std_output']:.6f}\")\n            log.debug(f\"  - Max output: {stats_info['max_output']:.6f}\")\n            log.debug(f\"  - Min output: {stats_info['min_output']:.6f}\")\n\n        else:\n            # Create empty DataFrame with correct columns\n            columns = ['timestamp', 'measurement', 'setpoint', 'control_action']\n            empty_df = pd.DataFrame(columns=columns)\n            empty_df.to_csv(csv_file, index=False)\n\n            log.warning(\"No simulation data collected - saved empty files with headers\")\n\n        return str(csv_file)\n\n    except Exception as e:\n        log.error(f\"Failed to save simulation results: {e}\")\n        raise\n</code></pre>"},{"location":"reference/config/","title":"Configuration Reference","text":"<p>This page documents the configuration system for Nanotherm.</p>"},{"location":"reference/config/#configuration-loading","title":"Configuration Loading","text":"<p>The <code>config</code> module handles loading and parsing of application settings from TOML files:</p>"},{"location":"reference/config/#nanotherm.config.config","title":"config","text":"<p>Configuration loader module for nanotherm.</p> <p>This module handles loading and parsing of the application configuration from TOML files. It provides functionality to read and validate configuration settings used throughout the application.</p>"},{"location":"reference/config/#nanotherm.config.config.load_config","title":"load_config","text":"<pre><code>load_config(platform: HardwareType) -&gt; Dict[str, Any]\n</code></pre> <p>Load application configuration from TOML file.</p> <p>Returns:</p> Type Description <code>Dict[str, Any]</code> <p>Dict[str, Any]: Configuration dictionary containing all application settings</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If the config.toml file cannot be found</p> <code>TomlDecodeError</code> <p>If the TOML file contains syntax errors</p> Source code in <code>src/nanotherm/config/config.py</code> <pre><code>def load_config(platform: HardwareType) -&gt; Dict[str, Any]:\n    \"\"\"\n    Load application configuration from TOML file.\n\n    Returns:\n        Dict[str, Any]: Configuration dictionary containing all application settings\n\n    Raises:\n        FileNotFoundError: If the config.toml file cannot be found\n        toml.TomlDecodeError: If the TOML file contains syntax errors\n    \"\"\"\n    # The file is opened in normal text mode, e.g., \"r\"\n    try:\n        with open(TOML_PATH, \"r\") as f:\n            config_data = toml.load(f)\n\n        config_data['platform'] = platform\n        return config_data\n\n    except FileNotFoundError as e:\n        #print(e)\n        logging.basicConfig(level=logging.CRITICAL)\n        log = logging.getLogger(__name__)\n        log.critical(\"Error: config.toml not found.\")\n        exit(1)\n    except toml.TomlDecodeError as e:\n        print(e)\n        logging.basicConfig(level=logging.CRITICAL)\n        log = logging.getLogger(__name__)\n        log.critical(\"Error: Could not decode the TOML file. Check for syntax errors.\")\n        exit(1)\n</code></pre>"},{"location":"reference/config/#logging-configuration","title":"Logging Configuration","text":"<p>The <code>logger</code> module provides logging setup with support for different environments:</p>"},{"location":"reference/config/#nanotherm.config.logger","title":"logger","text":"<p>Logging configuration module for nanotherm.</p> <p>This module provides logging setup functionality with support for different logging levels, file rotation, and format configuration.</p>"},{"location":"reference/config/#nanotherm.config.logger.setup_logging","title":"setup_logging","text":"<pre><code>setup_logging(config: Dict[str, Any]) -&gt; None\n</code></pre> <p>Configure the application's logging system.</p> Source code in <code>src/nanotherm/config/logger.py</code> <pre><code>def setup_logging(config: Dict[str, Any]) -&gt; None:\n    \"\"\"Configure the application's logging system.\"\"\"\n    level = logging.DEBUG if config[\"level\"] == \"DEBUG\" else logging.INFO\n    format = config[\"format\"]\n    date_format = config['date_format']\n\n    # Silence matplotlib and PIL debug messages\n    logging.getLogger('matplotlib').setLevel(logging.WARNING)\n    logging.getLogger('PIL').setLevel(logging.WARNING)\n\n    # Split format string to insert colors only around levelname\n    parts = format.split(\"[%(levelname)s]\")\n    colored_format = (\n        f\"{parts[0]}%(log_color)s[%(levelname)s]%(reset)s{parts[1]}\"\n    )\n\n    # Console handler with colors\n    console_handler = logging.StreamHandler()\n    console_formatter = colorlog.ColoredFormatter(\n        fmt=colored_format,\n        datefmt=date_format,\n        log_colors={\n            'DEBUG':    'cyan',\n            'INFO':     'white',\n            'WARNING': 'yellow',\n            'ERROR':    'light_red',\n            'CRITICAL': 'light_yellow',\n        },\n        secondary_log_colors={\n            'message': {\n                'CRITICAL': 'red'\n            }\n        }\n    )\n    console_handler.setFormatter(console_formatter)\n\n    # Direct configuration of root logger\n    root_logger = logging.getLogger()\n    root_logger.setLevel(level)\n    root_logger.addHandler(console_handler)\n\n    # File handler with plain formatting\n    if config[\"level\"] == \"DEBUG\":\n        # write debug logs into project-root/logs/debug.log\n        project_root = Path(__file__).parent.parent.parent.parent\n        log_dir = project_root / \"logs\"\n        log_dir.mkdir(exist_ok=True)\n        debug_log = log_dir / \"debug.log\"\n\n        file_handler = RotatingFileHandler(\n            debug_log,\n            maxBytes=1_000_000,\n            backupCount=3\n        )\n        file_handler.setFormatter(logging.Formatter(fmt=format, datefmt=date_format))\n        root_logger.addHandler(file_handler)\n\n    elif config[\"level\"] == \"deploy\":\n        # production logging to /var/log/myproj.log\n        file_handler = RotatingFileHandler(\n            \"/var/log/myproj.log\",\n            maxBytes=1_000_000,\n            backupCount=3\n        )\n        file_handler.setFormatter(logging.Formatter(fmt=format, datefmt=date_format))\n        root_logger.addHandler(file_handler)\n</code></pre>"},{"location":"reference/config/#nanotherm.config.logger.test_setup","title":"test_setup","text":"<pre><code>test_setup()\n</code></pre> <p>For testing only</p> Source code in <code>src/nanotherm/config/logger.py</code> <pre><code>def test_setup():\n    \"\"\"\n    For testing only\n    \"\"\"\n\n    log = logging.getLogger()\n    log.info('This is a INFO log')\n    log.debug('This is a DEBUG log')\n    log.warning('This is a WARNING log')\n    log.error('This is a ERROR log')\n    log.critical('This is a CRITICAL log')\n</code></pre>"},{"location":"reference/config/#configuration-schema","title":"Configuration Schema","text":""},{"location":"reference/config/#logging-section","title":"Logging Section","text":"<pre><code>[logging]\nlevel = \"DEBUG\"  # or \"deploy\"\nformat = \"%(asctime)s [%(levelname)s] %(name)s: %(message)s\"\ndate_format = \"%d-%m-%Y %H:%M:%S\"\n</code></pre>"},{"location":"reference/config/#pid-controller-section","title":"PID Controller Section","text":"<pre><code>[pid]\nkp = 1.0  # Proportional gain\nki = 0.2  # Integral gain\nkd = 0.05 # Derivative gain\n</code></pre>"},{"location":"reference/main/","title":"Entry Point Reference","text":"<p>This page documents the main entry point for the Nanotherm application.</p>"},{"location":"reference/main/#overview","title":"Overview","text":"<p>The <code>__main__</code> module serves as the primary entry point for the Nanotherm application. It handles: - Configuration loading and validation  - Logging setup - Application initialization - Error handling and graceful shutdown</p>"},{"location":"reference/main/#module-documentation","title":"Module Documentation","text":""},{"location":"reference/main/#nanotherm.__main__","title":"__main__","text":"<p>Main module for the nanotherm application.</p> <p>This module handles the application's entry point and logging configuration. It provides functionality to set up logging based on different running modes (debug or deployment).</p>"},{"location":"reference/main/#nanotherm.__main__.main","title":"main","text":"<pre><code>main()\n</code></pre> <p>Main entry point for the nanotherm application.</p> <p>Initializes the application and starts the main processing loop.</p> Source code in <code>src/nanotherm/__main__.py</code> <pre><code>def main():\n    \"\"\"\n    Main entry point for the nanotherm application.\n\n    Initializes the application and starts the main processing loop.\n    \"\"\"\n\n    platform = Platform.get_hardware_type()\n    config = load_config(platform)\n    setup_logging(config['logging'])\n\n    logger = logging.getLogger(__name__)\n    logger.info(\"Finished setup.\")\n\n    app = App(config)\n    app.run()\n</code></pre>"},{"location":"reference/platform/","title":"Platform Detection Module","text":"<p>This module provides enums and utility classes to detect and identify the current hardware platform and operating system the code is running on.</p>"},{"location":"reference/platform/#source-reference","title":"Source Reference","text":""},{"location":"reference/platform/#nanotherm.config.platform","title":"platform","text":"<p>Platform detection module for hardware and operating system identification.</p> <p>This module provides enums and utility classes to detect and identify the current hardware platform and operating system the code is running on.</p>"},{"location":"reference/platform/#nanotherm.config.platform.HardwareType","title":"HardwareType","text":"<p>               Bases: <code>Enum</code></p> <p>Enum representing different types of hardware platforms.</p> Source code in <code>src/nanotherm/config/platform.py</code> <pre><code>class HardwareType(Enum):\n    \"\"\"Enum representing different types of hardware platforms.\"\"\"\n    RASPBERRY_PI  = \"raspberry\"\n    DESKTOP = \"desktop\"\n    MCU = \"mcu\"\n    UNKNOWN = \"unknown\"\n</code></pre>"},{"location":"reference/platform/#nanotherm.config.platform.SystemPlatform","title":"SystemPlatform","text":"<p>               Bases: <code>Enum</code></p> <p>Enum representing different operating system platforms.</p> Source code in <code>src/nanotherm/config/platform.py</code> <pre><code>class SystemPlatform(Enum):\n    \"\"\"Enum representing different operating system platforms.\"\"\"\n    LINUX = \"linux\"\n    WINDOWS = \"windows\"\n    UNKNOWN = \"unknown\"\n</code></pre>"},{"location":"reference/platform/#nanotherm.config.platform.Platform","title":"Platform","text":"<p>Utility class for platform detection and identification.</p> Source code in <code>src/nanotherm/config/platform.py</code> <pre><code>class Platform:\n    \"\"\"Utility class for platform detection and identification.\"\"\"\n\n    @staticmethod\n    def get_system() -&gt; SystemPlatform:\n        \"\"\"Detect the current operating system.\n\n        Returns:\n            SystemPlatform: The detected operating system platform.\n        \"\"\"\n        system = platform.system().lower()\n        if system == \"linux\":\n            return SystemPlatform.LINUX\n        elif system == \"windows\":\n            return SystemPlatform.WINDOWS\n        return SystemPlatform.UNKNOWN\n\n    @staticmethod\n    def get_hardware_type() -&gt; HardwareType:\n        \"\"\"Detect the current hardware platform.\n\n        Returns:\n            HardwareType: The detected hardware platform type.\n        \"\"\"\n        node = platform.node().lower()\n        machine = platform.machine().lower()\n\n        # Check for Raspberry Pi based on node and machine\n        if \"raspberrypi\" in node and  machine in (\"armv6l\", \"armv7l\", \"armv8l\", \"aarch64\"):\n            return HardwareType.RASPBERRY_PI\n        elif Platform.get_system() in [SystemPlatform.LINUX, SystemPlatform.WINDOWS]:\n            return HardwareType.DESKTOP\n        else:\n            return HardwareType.UNKNOWN\n</code></pre>"},{"location":"reference/platform/#nanotherm.config.platform.Platform.get_system","title":"get_system  <code>staticmethod</code>","text":"<pre><code>get_system() -&gt; SystemPlatform\n</code></pre> <p>Detect the current operating system.</p> <p>Returns:</p> Name Type Description <code>SystemPlatform</code> <code>SystemPlatform</code> <p>The detected operating system platform.</p> Source code in <code>src/nanotherm/config/platform.py</code> <pre><code>@staticmethod\ndef get_system() -&gt; SystemPlatform:\n    \"\"\"Detect the current operating system.\n\n    Returns:\n        SystemPlatform: The detected operating system platform.\n    \"\"\"\n    system = platform.system().lower()\n    if system == \"linux\":\n        return SystemPlatform.LINUX\n    elif system == \"windows\":\n        return SystemPlatform.WINDOWS\n    return SystemPlatform.UNKNOWN\n</code></pre>"},{"location":"reference/platform/#nanotherm.config.platform.Platform.get_hardware_type","title":"get_hardware_type  <code>staticmethod</code>","text":"<pre><code>get_hardware_type() -&gt; HardwareType\n</code></pre> <p>Detect the current hardware platform.</p> <p>Returns:</p> Name Type Description <code>HardwareType</code> <code>HardwareType</code> <p>The detected hardware platform type.</p> Source code in <code>src/nanotherm/config/platform.py</code> <pre><code>@staticmethod\ndef get_hardware_type() -&gt; HardwareType:\n    \"\"\"Detect the current hardware platform.\n\n    Returns:\n        HardwareType: The detected hardware platform type.\n    \"\"\"\n    node = platform.node().lower()\n    machine = platform.machine().lower()\n\n    # Check for Raspberry Pi based on node and machine\n    if \"raspberrypi\" in node and  machine in (\"armv6l\", \"armv7l\", \"armv8l\", \"aarch64\"):\n        return HardwareType.RASPBERRY_PI\n    elif Platform.get_system() in [SystemPlatform.LINUX, SystemPlatform.WINDOWS]:\n        return HardwareType.DESKTOP\n    else:\n        return HardwareType.UNKNOWN\n</code></pre>"},{"location":"reference/platform/#usage-example","title":"Usage Example","text":"<pre><code>from nanotherm.config.platform import Platform\n\nsystem = Platform.get_system()\nhardware = Platform.get_hardware_type()\n\nprint(f\"System platform: {system}\")\nprint(f\"Hardware type: {hardware}\")\n</code></pre>"},{"location":"reference/platform/#enums","title":"Enums","text":"<ul> <li><code>HardwareType</code>: Types of hardware platforms (e.g., Raspberry Pi, Desktop, MCU, Unknown)</li> <li><code>SystemPlatform</code>: Types of operating systems (e.g., Linux, Windows, Unknown)</li> </ul>"},{"location":"reference/platform/#classes","title":"Classes","text":"<ul> <li><code>Platform</code>: Utility class for platform detection and identification.</li> </ul>"},{"location":"reference/core/domain/pid_params/","title":"Transfer Function Domain Models","text":"<p>This module defines data structures for PID parameters used in Control System Modeling.</p>"},{"location":"reference/core/domain/pid_params/#code-reference","title":"Code Reference","text":""},{"location":"reference/core/domain/pid_params/#nanotherm.core.domain.pid_params","title":"pid_params","text":""},{"location":"reference/core/domain/pid_params/#nanotherm.core.domain.pid_params.PIDParams","title":"PIDParams  <code>dataclass</code>","text":"<p>Holds the proportional, integral, and derivative gains for a PID controller.</p> Source code in <code>src/nanotherm/core/domain/pid_params.py</code> <pre><code>@dataclass(frozen=True)\nclass PIDParams:\n    \"\"\"\n    Holds the proportional, integral, and derivative gains\n    for a PID controller.\n    \"\"\"\n    kp: float\n    ki: float\n    kd: float\n\n    def __str__(self) -&gt; str:\n        \"\"\"Return a user-friendly string representation of PID parameters.\"\"\"\n        return f\"PID(Kp={self.kp}, Ki={self.ki}, Kd={self.kd})\"\n\n    def __repr__(self) -&gt; str:\n        \"\"\"Return a detailed string representation for debugging.\"\"\"\n        return f\"PIDParams(kp={self.kp}, ki={self.ki}, kd={self.kd})\"\n</code></pre>"},{"location":"reference/core/domain/pid_params/#nanotherm.core.domain.pid_params.PIDParams.__str__","title":"__str__","text":"<pre><code>__str__() -&gt; str\n</code></pre> <p>Return a user-friendly string representation of PID parameters.</p> Source code in <code>src/nanotherm/core/domain/pid_params.py</code> <pre><code>def __str__(self) -&gt; str:\n    \"\"\"Return a user-friendly string representation of PID parameters.\"\"\"\n    return f\"PID(Kp={self.kp}, Ki={self.ki}, Kd={self.kd})\"\n</code></pre>"},{"location":"reference/core/domain/pid_params/#nanotherm.core.domain.pid_params.PIDParams.__repr__","title":"__repr__","text":"<pre><code>__repr__() -&gt; str\n</code></pre> <p>Return a detailed string representation for debugging.</p> Source code in <code>src/nanotherm/core/domain/pid_params.py</code> <pre><code>def __repr__(self) -&gt; str:\n    \"\"\"Return a detailed string representation for debugging.\"\"\"\n    return f\"PIDParams(kp={self.kp}, ki={self.ki}, kd={self.kd})\"\n</code></pre>"},{"location":"reference/core/domain/pid_params/#classes","title":"Classes","text":"<ul> <li><code>PIDParams</code>: Holds the proportional, integral, and derivative gains for a PID controller.</li> </ul> <p>Imported by: - App - PID Controller</p>"},{"location":"reference/core/domain/transferFunction/","title":"Transfer Function Domain Models","text":"<p>This module defines data structures for representing transfer functions, used in control system modeling.</p>"},{"location":"reference/core/domain/transferFunction/#source-reference","title":"Source Reference","text":""},{"location":"reference/core/domain/transferFunction/#nanotherm.core.domain.transferFunction","title":"transferFunction","text":""},{"location":"reference/core/domain/transferFunction/#nanotherm.core.domain.transferFunction.ITransferFunction","title":"ITransferFunction  <code>dataclass</code>","text":"<p>Abstract representation of a transfer function.</p> <p>Attributes:</p> Name Type Description <code>num</code> <code>Sequence[float]</code> <p>Numerator coefficients of the transfer function.</p> <code>den</code> <code>Sequence[float]</code> <p>Denominator coefficients of the transfer function.</p> Source code in <code>src/nanotherm/core/domain/transferFunction.py</code> <pre><code>@dataclass(frozen=True)\nclass ITransferFunction:\n    \"\"\"\n    Abstract representation of a transfer function.\n\n    Attributes:\n        num (Sequence[float]): Numerator coefficients of the transfer function.\n        den (Sequence[float]): Denominator coefficients of the transfer function.\n    \"\"\"\n    num: Sequence[float]\n    den: Sequence[float]\n</code></pre>"},{"location":"reference/core/domain/transferFunction/#classes","title":"Classes","text":"<ul> <li><code>ITransferFunction</code>: Abstract representation of a transfer function, with numerator and denominator coefficients.</li> </ul> <p>Imported by: - Discrete Transfer Function</p>"},{"location":"reference/core/entities/DTransferFunction/","title":"Discrete Transfer Function Entity","text":"<p>This module provides a dataclass for representing a discrete transfer function, including numerator, denominator, and sampling time.</p>"},{"location":"reference/core/entities/DTransferFunction/#code-reference","title":"Code Reference","text":""},{"location":"reference/core/entities/DTransferFunction/#nanotherm.core.entities.DTransferFunction","title":"DTransferFunction","text":""},{"location":"reference/core/entities/DTransferFunction/#nanotherm.core.entities.DTransferFunction.DiscreteTransferFunction","title":"DiscreteTransferFunction  <code>dataclass</code>","text":"<p>               Bases: <code>ITransferFunction</code></p> <p>Discrete transfer function with a specified sampling time.</p> <p>Attributes:</p> Name Type Description <code>num</code> <code>list or array</code> <p>Numerator coefficients of the transfer function.</p> <code>den</code> <code>list or array</code> <p>Denominator coefficients of the transfer function.</p> <code>Ts</code> <code>float</code> <p>Sampling time of the discrete system.</p> Source code in <code>src/nanotherm/core/entities/DTransferFunction.py</code> <pre><code>@dataclass(frozen=True)\nclass DiscreteTransferFunction(ITransferFunction):\n    \"\"\"\n    Discrete transfer function with a specified sampling time.\n\n    Attributes:\n        num (list or array): Numerator coefficients of the transfer function.\n        den (list or array): Denominator coefficients of the transfer function.\n        Ts (float): Sampling time of the discrete system.\n    \"\"\"\n    num: list\n    den: list\n    Ts: float\n\n    @property\n    def tf(self):\n        return ctrl.TransferFunction(self.num, self.den, self.Ts)\n</code></pre>"},{"location":"reference/core/entities/DTransferFunction/#classes","title":"Classes","text":"<ul> <li><code>DiscreteTransferFunction</code>: Discrete transfer function with numerator, denominator, and sampling time.</li> </ul> <p>Imported by: - App - PID Controller</p>"},{"location":"reference/core/entities/controller/","title":"Controller Interface","text":"<p>This module defines the abstract base class for controllers in the control system, specifying the required interface for Clean Architecture.</p>"},{"location":"reference/core/entities/controller/#code-reference","title":"Code Reference","text":""},{"location":"reference/core/entities/controller/#nanotherm.core.entities.controller","title":"controller","text":"<p>Controller interface definition.</p> <p>This module defines the abstract base class that all controllers must implement.</p>"},{"location":"reference/core/entities/controller/#nanotherm.core.entities.controller.IController","title":"IController","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class for control system implementations.</p> <p>This interface defines the contract that all controllers must fulfill to be usable within the control loop.</p> Source code in <code>src/nanotherm/core/entities/controller.py</code> <pre><code>class IController(ABC):\n    \"\"\"\n    Abstract base class for control system implementations.\n\n    This interface defines the contract that all controllers must fulfill\n    to be usable within the control loop.\n    \"\"\"\n\n    @abstractmethod\n    def _build_tf(self, ) -&gt; ctrl.TransferFunction:\n        pass\n\n    @abstractmethod\n    def compute(self, measurement: float, timestamp: float) -&gt; float:\n        \"\"\"\n        Compute the control action based on current measurement.\n\n        Args:\n            measurement: Current value of the controlled variable\n            timestamp: Current time in seconds since the start of the control loop\n\n        Returns:\n            Control action to be applied to the system\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def step_response(self, t_final: float) -&gt; Tuple[np.ndarray, np.ndarray]:\n        \"\"\"\n        Purely for studying purposes\n        \"\"\"\n        pass\n</code></pre>"},{"location":"reference/core/entities/controller/#nanotherm.core.entities.controller.IController.compute","title":"compute  <code>abstractmethod</code>","text":"<pre><code>compute(measurement: float, timestamp: float) -&gt; float\n</code></pre> <p>Compute the control action based on current measurement.</p> <p>Parameters:</p> Name Type Description Default <code>measurement</code> <code>float</code> <p>Current value of the controlled variable</p> required <code>timestamp</code> <code>float</code> <p>Current time in seconds since the start of the control loop</p> required <p>Returns:</p> Type Description <code>float</code> <p>Control action to be applied to the system</p> Source code in <code>src/nanotherm/core/entities/controller.py</code> <pre><code>@abstractmethod\ndef compute(self, measurement: float, timestamp: float) -&gt; float:\n    \"\"\"\n    Compute the control action based on current measurement.\n\n    Args:\n        measurement: Current value of the controlled variable\n        timestamp: Current time in seconds since the start of the control loop\n\n    Returns:\n        Control action to be applied to the system\n    \"\"\"\n    pass\n</code></pre>"},{"location":"reference/core/entities/controller/#nanotherm.core.entities.controller.IController.step_response","title":"step_response  <code>abstractmethod</code>","text":"<pre><code>step_response(t_final: float) -&gt; Tuple[np.ndarray, np.ndarray]\n</code></pre> <p>Purely for studying purposes</p> Source code in <code>src/nanotherm/core/entities/controller.py</code> <pre><code>@abstractmethod\ndef step_response(self, t_final: float) -&gt; Tuple[np.ndarray, np.ndarray]:\n    \"\"\"\n    Purely for studying purposes\n    \"\"\"\n    pass\n</code></pre>"},{"location":"reference/core/entities/controller/#classes","title":"Classes","text":"<ul> <li><code>IController</code>: Abstract base class for control system implementations.</li> </ul> <p>Imported by: - PID Controller</p>"},{"location":"reference/core/entities/input_gateway/","title":"Input Gateway Interface","text":"<p>This module defines the abstract base class for hardware abstraction layer (HAL) gateways, specifying the required interface for reading and writing hardware or simulated values.</p>"},{"location":"reference/core/entities/input_gateway/#code-reference","title":"Code Reference","text":""},{"location":"reference/core/entities/input_gateway/#nanotherm.core.entities.inputGateway","title":"inputGateway","text":""},{"location":"reference/core/entities/input_gateway/#nanotherm.core.entities.inputGateway.I_HALGateway","title":"I_HALGateway","text":"<p>               Bases: <code>ABC</code></p> <p>Interface for a Hardware Abstraction Layer (HAL) Gateway.</p> <p>This abstract base class defines the contract for reading input values and writing output values to hardware devices or their software representations. Implementations should provide concrete methods for interacting with specific hardware or simulation environments.</p> Source code in <code>src/nanotherm/core/entities/inputGateway.py</code> <pre><code>class I_HALGateway(ABC):\n    \"\"\"\n    Interface for a Hardware Abstraction Layer (HAL) Gateway.\n\n    This abstract base class defines the contract for reading input values and writing output values\n    to hardware devices or their software representations. Implementations should provide concrete\n    methods for interacting with specific hardware or simulation environments.\n    \"\"\"\n\n    @abstractmethod\n    def read_value(self, index_: Optional[int] = None, input_id: str | int = ...) -&gt; float:\n        \"\"\"\n        Read a value from a hardware input or from a specified index.\n\n        Args:\n            index_ (Optional[int]): The index of the input to read. \n            input_id (Optional[str]): The id of the column to be read\n\n        Returns:\n            float: The value read from the hardware input or its software representation.\n        \"\"\"\n        pass\n\n    @abstractmethod\n    def write_value(self, output_id: str, value: float) -&gt; None:\n        \"\"\"\n        Write a value to a hardware output.\n\n        Args:\n            output_id (str): The identifier of the output to write to.\n            value (float): The value to write to the output.\n\n        Returns:\n            None\n        \"\"\"\n        pass\n</code></pre>"},{"location":"reference/core/entities/input_gateway/#nanotherm.core.entities.inputGateway.I_HALGateway.read_value","title":"read_value  <code>abstractmethod</code>","text":"<pre><code>read_value(index_: Optional[int] = None, input_id: str | int = ...) -&gt; float\n</code></pre> <p>Read a value from a hardware input or from a specified index.</p> <p>Parameters:</p> Name Type Description Default <code>index_</code> <code>Optional[int]</code> <p>The index of the input to read. </p> <code>None</code> <code>input_id</code> <code>Optional[str]</code> <p>The id of the column to be read</p> <code>...</code> <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>The value read from the hardware input or its software representation.</p> Source code in <code>src/nanotherm/core/entities/inputGateway.py</code> <pre><code>@abstractmethod\ndef read_value(self, index_: Optional[int] = None, input_id: str | int = ...) -&gt; float:\n    \"\"\"\n    Read a value from a hardware input or from a specified index.\n\n    Args:\n        index_ (Optional[int]): The index of the input to read. \n        input_id (Optional[str]): The id of the column to be read\n\n    Returns:\n        float: The value read from the hardware input or its software representation.\n    \"\"\"\n    pass\n</code></pre>"},{"location":"reference/core/entities/input_gateway/#nanotherm.core.entities.inputGateway.I_HALGateway.write_value","title":"write_value  <code>abstractmethod</code>","text":"<pre><code>write_value(output_id: str, value: float) -&gt; None\n</code></pre> <p>Write a value to a hardware output.</p> <p>Parameters:</p> Name Type Description Default <code>output_id</code> <code>str</code> <p>The identifier of the output to write to.</p> required <code>value</code> <code>float</code> <p>The value to write to the output.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/nanotherm/core/entities/inputGateway.py</code> <pre><code>@abstractmethod\ndef write_value(self, output_id: str, value: float) -&gt; None:\n    \"\"\"\n    Write a value to a hardware output.\n\n    Args:\n        output_id (str): The identifier of the output to write to.\n        value (float): The value to write to the output.\n\n    Returns:\n        None\n    \"\"\"\n    pass\n</code></pre>"},{"location":"reference/core/entities/input_gateway/#classes","title":"Classes","text":""},{"location":"reference/core/entities/input_gateway/#i_halgateway","title":"I_HALGateway","text":"<p>Abstract base class for hardware input/output gateways.</p> <p>Imported by: - Simulation Gateway</p>"},{"location":"reference/core/infrastructure/gateways/","title":"Data Gateway","text":"<p>Warning</p> <p>This module is currently a placeholder. Implementation of data gateway classes for hardware integration is planned.</p> <p>This module defines the interface for data gateways used to interact with hardware.</p>"},{"location":"reference/core/infrastructure/gateways/#code-reference","title":"Code Reference","text":""},{"location":"reference/core/infrastructure/gateways/#nanotherm.infrastructure.gateways.data_gateway","title":"data_gateway","text":""},{"location":"reference/core/infrastructure/gateways/#classes","title":"Classes","text":"<p>Note: The canonical documentation for <code>I_HALGateway</code> is in Input Gateway Interface.</p> <p>Importes: - Simulation Gateway</p>"},{"location":"reference/core/infrastructure/pid_controller/","title":"PID Controller","text":"<p>This module provides the implementation of the discrete-time PID controller.</p>"},{"location":"reference/core/infrastructure/pid_controller/#code-reference","title":"Code Reference","text":""},{"location":"reference/core/infrastructure/pid_controller/#nanotherm.infrastructure.controllers.pid_controller","title":"pid_controller","text":"<p>PID controller implementation.</p> <p>Provides a discrete-time PID controller using transfer function representation.</p>"},{"location":"reference/core/infrastructure/pid_controller/#nanotherm.infrastructure.controllers.pid_controller.PIDController","title":"PIDController","text":"<p>               Bases: <code>IController</code></p> <p>A discrete-time PID controller implemented as a transfer function C(z).</p> Source code in <code>src/nanotherm/infrastructure/controllers/pid_controller.py</code> <pre><code>class PIDController(IController):\n    \"\"\"\n    A discrete-time PID controller implemented as a transfer function C(z).\n    \"\"\"\n\n    def __init__(self, gains: PIDParams, plant: DiscreteTransferFunction, setpoint: float = 1.0, Ts: float = 1.0) -&gt; None:\n        self.kp = gains.kp\n        self.ki = gains.ki\n        self.kd = gains.kd\n        self.plant = plant\n        self._setpoint = setpoint\n        self.Ts = Ts\n        self._tf = self._build_tf()\n        self._last_error = 0.0\n        self._last_time = None \n        self._integral = 0.0\n\n        log.debug(f'Controller instance created with setpoint = {self._setpoint} and sampling time = {self.Ts}')\n\n    def _build_tf(self) -&gt; ctrl.TransferFunction:\n        \"\"\"\n\n\n        Returns:\n            The complete discrete-time PID controller transfer function\n        \"\"\"\n        # Proportional term\n        C_p = ctrl.TransferFunction([self.kp], [1], self.Ts)\n\n        # Integral term: Ki * Ts / (z - 1)\n        C_i = ctrl.TransferFunction([self.ki * self.Ts], [1, -1], self.Ts)\n\n        # Derivative term: Kd * (z - 1) / Ts\n        C_d = ctrl.TransferFunction([self.kd, -self.kd], [self.Ts, 0], self.Ts)\n\n        Hz = C_p + C_i + C_d # Full PID Transfer Function\n        Gz = self.plant.tf   # Plant transfer function\n\n        return ctrl.feedback(Hz * Gz, 1) # type: ignore\n\n    @property\n    def transferFunction(self) -&gt; ctrl.TransferFunction:\n        \"\"\"\n        Get the controller's transfer function representation.\n\n        Returns:\n            The discrete-time transfer function C(z) of the controller\n        \"\"\"\n        return self._tf\n\n    @property\n    def setpoint(self) -&gt; float:\n        \"\"\"\n        Get the current target value for the controlled variable.\n\n        Returns:\n            The current setpoint value\n        \"\"\"\n        return self._setpoint\n\n    @setpoint.setter\n    def setpoint(self, value: float) -&gt; None:\n        \"\"\"\n        Set a new target value and reset internal controller states.\n\n        Args:\n            value: New setpoint value for the controller\n\n        Notes:\n            Resets integral and error memory to avoid bumps in control action\n        \"\"\"\n        self._setpoint = value\n        self._last_error = 0.0\n        self._integral = 0.0\n\n    def compute(self, measurement: float, timestamp: float) -&gt; float:\n        \"\"\"\n        Compute control action based on current measurement.\n\n        Args:\n            measurement: Current value of the controlled variable\n\n        Returns:\n            Control action to be applied to the system\n\n        Notes:\n            Uses the transfer function representation to compute the\n            control action from the current error signal\n        \"\"\"\n\n        dt = timestamp - self._last_time if self._last_time is not None else self.Ts\n\n        error = self.setpoint - measurement\n\n        # PID computation\n        self._integral += error * dt\n        derivative = (error - self._last_error) / dt if self._last_error is not None else 0\n\n        u = self.kp * error + self.ki * self._integral + self.kd * derivative\n\n        self._last_error = error\n        self._last_time = timestamp\n        return u\n\n    def step_response(self, t_final: float) -&gt; Tuple[np.ndarray, np.ndarray]:\n        \"\"\"\n        Compute the step response of the controller.\n\n        Args:\n            t_final: Final time for the simulation in seconds\n\n        Returns:\n            A tuple containing:\n                - Time points array\n                - Controller output response array\n\n        Notes:\n            Useful for analyzing controller behavior and tuning\n        \"\"\"\n        t = np.arange(0, t_final + self._tf.dt, self._tf.dt) # type: ignore\n        return ctrl.step_response(self._tf, t) # type: ignore\n\n    def plot_step_response(self, t_final: float, show: bool = True, save: bool = False) -&gt; None:\n        \"\"\"\n        Plot and optionally save the step response of the controller.\n\n        Args:\n            t_final: Final time for the simulation in seconds\n            show: Whether to display the plot immediately (default: True)\n            save: Whether to save the plot to file (default: False)\n        \"\"\"\n        t, y = self.step_response(t_final)\n\n        plt.figure(figsize=(10, 6))\n        plt.plot(t, y, 'b-', linewidth=2, label='Controller Output')\n        plt.grid(True)\n        plt.title(f'PID Controller Step Response (Kp={self.kp}, Ki={self.ki}, Kd={self.kd})')\n        plt.xlabel('Time [s]')\n        plt.ylabel('Output')\n        plt.legend()\n\n        if save:\n            # Create plots directory in project root\n            project_root = Path(__file__).parent.parent.parent.parent.parent\n            plots_dir = project_root / \"plots\"\n            plots_dir.mkdir(exist_ok=True)\n\n            # Generate filename with timestamp and parameters\n            timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n            filename = f\"pid_step_response_{timestamp}.png\"\n            filepath = plots_dir / filename\n\n            plt.savefig(filepath)\n            log.info(f\"Step response plot saved to: {filepath.relative_to(project_root)}\")\n\n        if show:\n            plt.show()\n\n        plt.close()\n</code></pre>"},{"location":"reference/core/infrastructure/pid_controller/#nanotherm.infrastructure.controllers.pid_controller.PIDController.transferFunction","title":"transferFunction  <code>property</code>","text":"<pre><code>transferFunction: TransferFunction\n</code></pre> <p>Get the controller's transfer function representation.</p> <p>Returns:</p> Type Description <code>TransferFunction</code> <p>The discrete-time transfer function C(z) of the controller</p>"},{"location":"reference/core/infrastructure/pid_controller/#nanotherm.infrastructure.controllers.pid_controller.PIDController.setpoint","title":"setpoint  <code>property</code> <code>writable</code>","text":"<pre><code>setpoint: float\n</code></pre> <p>Get the current target value for the controlled variable.</p> <p>Returns:</p> Type Description <code>float</code> <p>The current setpoint value</p>"},{"location":"reference/core/infrastructure/pid_controller/#nanotherm.infrastructure.controllers.pid_controller.PIDController.compute","title":"compute","text":"<pre><code>compute(measurement: float, timestamp: float) -&gt; float\n</code></pre> <p>Compute control action based on current measurement.</p> <p>Parameters:</p> Name Type Description Default <code>measurement</code> <code>float</code> <p>Current value of the controlled variable</p> required <p>Returns:</p> Type Description <code>float</code> <p>Control action to be applied to the system</p> Notes <p>Uses the transfer function representation to compute the control action from the current error signal</p> Source code in <code>src/nanotherm/infrastructure/controllers/pid_controller.py</code> <pre><code>def compute(self, measurement: float, timestamp: float) -&gt; float:\n    \"\"\"\n    Compute control action based on current measurement.\n\n    Args:\n        measurement: Current value of the controlled variable\n\n    Returns:\n        Control action to be applied to the system\n\n    Notes:\n        Uses the transfer function representation to compute the\n        control action from the current error signal\n    \"\"\"\n\n    dt = timestamp - self._last_time if self._last_time is not None else self.Ts\n\n    error = self.setpoint - measurement\n\n    # PID computation\n    self._integral += error * dt\n    derivative = (error - self._last_error) / dt if self._last_error is not None else 0\n\n    u = self.kp * error + self.ki * self._integral + self.kd * derivative\n\n    self._last_error = error\n    self._last_time = timestamp\n    return u\n</code></pre>"},{"location":"reference/core/infrastructure/pid_controller/#nanotherm.infrastructure.controllers.pid_controller.PIDController.step_response","title":"step_response","text":"<pre><code>step_response(t_final: float) -&gt; Tuple[np.ndarray, np.ndarray]\n</code></pre> <p>Compute the step response of the controller.</p> <p>Parameters:</p> Name Type Description Default <code>t_final</code> <code>float</code> <p>Final time for the simulation in seconds</p> required <p>Returns:</p> Type Description <code>Tuple[ndarray, ndarray]</code> <p>A tuple containing: - Time points array - Controller output response array</p> Notes <p>Useful for analyzing controller behavior and tuning</p> Source code in <code>src/nanotherm/infrastructure/controllers/pid_controller.py</code> <pre><code>def step_response(self, t_final: float) -&gt; Tuple[np.ndarray, np.ndarray]:\n    \"\"\"\n    Compute the step response of the controller.\n\n    Args:\n        t_final: Final time for the simulation in seconds\n\n    Returns:\n        A tuple containing:\n            - Time points array\n            - Controller output response array\n\n    Notes:\n        Useful for analyzing controller behavior and tuning\n    \"\"\"\n    t = np.arange(0, t_final + self._tf.dt, self._tf.dt) # type: ignore\n    return ctrl.step_response(self._tf, t) # type: ignore\n</code></pre>"},{"location":"reference/core/infrastructure/pid_controller/#nanotherm.infrastructure.controllers.pid_controller.PIDController.plot_step_response","title":"plot_step_response","text":"<pre><code>plot_step_response(t_final: float, show: bool = True, save: bool = False) -&gt; None\n</code></pre> <p>Plot and optionally save the step response of the controller.</p> <p>Parameters:</p> Name Type Description Default <code>t_final</code> <code>float</code> <p>Final time for the simulation in seconds</p> required <code>show</code> <code>bool</code> <p>Whether to display the plot immediately (default: True)</p> <code>True</code> <code>save</code> <code>bool</code> <p>Whether to save the plot to file (default: False)</p> <code>False</code> Source code in <code>src/nanotherm/infrastructure/controllers/pid_controller.py</code> <pre><code>def plot_step_response(self, t_final: float, show: bool = True, save: bool = False) -&gt; None:\n    \"\"\"\n    Plot and optionally save the step response of the controller.\n\n    Args:\n        t_final: Final time for the simulation in seconds\n        show: Whether to display the plot immediately (default: True)\n        save: Whether to save the plot to file (default: False)\n    \"\"\"\n    t, y = self.step_response(t_final)\n\n    plt.figure(figsize=(10, 6))\n    plt.plot(t, y, 'b-', linewidth=2, label='Controller Output')\n    plt.grid(True)\n    plt.title(f'PID Controller Step Response (Kp={self.kp}, Ki={self.ki}, Kd={self.kd})')\n    plt.xlabel('Time [s]')\n    plt.ylabel('Output')\n    plt.legend()\n\n    if save:\n        # Create plots directory in project root\n        project_root = Path(__file__).parent.parent.parent.parent.parent\n        plots_dir = project_root / \"plots\"\n        plots_dir.mkdir(exist_ok=True)\n\n        # Generate filename with timestamp and parameters\n        timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n        filename = f\"pid_step_response_{timestamp}.png\"\n        filepath = plots_dir / filename\n\n        plt.savefig(filepath)\n        log.info(f\"Step response plot saved to: {filepath.relative_to(project_root)}\")\n\n    if show:\n        plt.show()\n\n    plt.close()\n</code></pre>"},{"location":"reference/core/infrastructure/pid_controller/#classes","title":"Classes","text":"<ul> <li><code>PIDController</code>: Discrete-time PID controller using transfer function representation.</li> </ul> <p>Imports: - Controller Interface - Discrete Transfer Function - PIDParams</p>"},{"location":"reference/core/infrastructure/simulation_gateway/","title":"Simulation Gateway","text":"<p>This module provides a simulated hardware abstraction layer (HAL) gateway using CSV data.</p>"},{"location":"reference/core/infrastructure/simulation_gateway/#code-reference","title":"Code Reference","text":""},{"location":"reference/core/infrastructure/simulation_gateway/#nanotherm.infrastructure.gateways.simul_data_gateway","title":"simul_data_gateway","text":""},{"location":"reference/core/infrastructure/simulation_gateway/#nanotherm.infrastructure.gateways.simul_data_gateway.CSV_HALGateway","title":"CSV_HALGateway","text":"<p>               Bases: <code>I_HALGateway</code></p> <p>Simulated Hardware Abstraction Layer (HAL) Gateway using CSV data.</p> <p>This class reads input values from a CSV file to simulate hardware input for testing and development.</p> Source code in <code>src/nanotherm/infrastructure/gateways/simul_data_gateway.py</code> <pre><code>class CSV_HALGateway(I_HALGateway):\n    \"\"\"\n    Simulated Hardware Abstraction Layer (HAL) Gateway using CSV data.\n\n    This class reads input values from a CSV file to simulate hardware input for testing and development.\n    \"\"\"\n\n    def __init__(self, path: str) -&gt; None:\n        \"\"\"\n        Initialize the simulated HAL gateway.\n\n        Args:\n            path (str): Path to the CSV file containing simulation data.\n        \"\"\"\n        self.__path = path\n        log.info(f\"Initializing simulated HAL. Reading inputs from {path}.\")\n        try:\n            simulated_data: pd.DataFrame = pd.read_csv(path)\n        except FileNotFoundError:\n            log.error(f\"Simulated CSV data file not found at {path}!\")\n            log.warning('Creating empty pd.DataFrame.')\n            simulated_data = pd.DataFrame()\n\n        if simulated_data.empty:\n            log.warning('Simulation data is EMPTY.')\n\n        self.__index: int = 0\n        self.__reach_end: bool = False\n        self._data_len: int = len(simulated_data)\n        self._simul_data: pd.DataFrame = simulated_data\n\n    def read_value(\n        self, \n        index_: Optional[int] = None, \n        input_id: Union[str, int] = 'Impedancia'\n    ) -&gt; float:\n        \"\"\"\n        Read a value from the simulated CSV data.\n\n        Args:\n            index_ (Optional[int]): The row index to read from. If None, reads the next row.\n            input_id (str | int): The column name or index to read from.\n\n        Returns:\n            float: The value read from the CSV data.\n\n        Raises:\n            AssertionError: If input types are incorrect.\n            IndexError: If the index is out of bounds.\n        \"\"\"\n        assert index_ is None or isinstance(index_, int), \"index_ should be of type 'int' or None\"\n        assert isinstance(input_id, (str, int)), \"input_id should be of type 'str' or 'int'\"\n\n        if self.__index == self._data_len-1:\n            log.info('No more data to be read from CSV file.')\n            log.debug(f'Final index read from {self.__path}: {self.__index}.')\n            self.__reach_end = True\n\n        if index_ is None:\n            index = self.__index\n            self.__index += 1\n        else:\n            index = index_\n\n        if index &gt;= self._data_len:\n            log.error(f'Index value [ {index} ] out of data bounds. Data length is {self._data_len}')\n            raise IndexError(f'Index {index} out of bounds for data length {self._data_len}')\n\n        current_row = self._simul_data.iloc[index]\n        value = current_row[input_id] \n        #log.debug(f'Value read from index {index}: {value}')\n\n        return float(value)\n\n    def write_value(self, output_id: str, value: float) -&gt; None:\n        \"\"\"\n        Simulate writing a value to a hardware output.\n\n        Args:\n            output_id (str): The identifier of the output to write to.\n            value (float): The value to write.\n\n        Returns:\n            None\n        \"\"\"\n        log.info(f\"Simulated writing {value} to: {output_id}\")\n\n    @property\n    def reach_end(self) -&gt; bool:\n        \"\"\"\n        Check if the end of the simulated data has been reached.\n\n        Returns:\n            bool: True if the end of the data has been reached, False otherwise.\n        \"\"\"\n        return self.__reach_end\n</code></pre>"},{"location":"reference/core/infrastructure/simulation_gateway/#nanotherm.infrastructure.gateways.simul_data_gateway.CSV_HALGateway.reach_end","title":"reach_end  <code>property</code>","text":"<pre><code>reach_end: bool\n</code></pre> <p>Check if the end of the simulated data has been reached.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the end of the data has been reached, False otherwise.</p>"},{"location":"reference/core/infrastructure/simulation_gateway/#nanotherm.infrastructure.gateways.simul_data_gateway.CSV_HALGateway.__init__","title":"__init__","text":"<pre><code>__init__(path: str) -&gt; None\n</code></pre> <p>Initialize the simulated HAL gateway.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str</code> <p>Path to the CSV file containing simulation data.</p> required Source code in <code>src/nanotherm/infrastructure/gateways/simul_data_gateway.py</code> <pre><code>def __init__(self, path: str) -&gt; None:\n    \"\"\"\n    Initialize the simulated HAL gateway.\n\n    Args:\n        path (str): Path to the CSV file containing simulation data.\n    \"\"\"\n    self.__path = path\n    log.info(f\"Initializing simulated HAL. Reading inputs from {path}.\")\n    try:\n        simulated_data: pd.DataFrame = pd.read_csv(path)\n    except FileNotFoundError:\n        log.error(f\"Simulated CSV data file not found at {path}!\")\n        log.warning('Creating empty pd.DataFrame.')\n        simulated_data = pd.DataFrame()\n\n    if simulated_data.empty:\n        log.warning('Simulation data is EMPTY.')\n\n    self.__index: int = 0\n    self.__reach_end: bool = False\n    self._data_len: int = len(simulated_data)\n    self._simul_data: pd.DataFrame = simulated_data\n</code></pre>"},{"location":"reference/core/infrastructure/simulation_gateway/#nanotherm.infrastructure.gateways.simul_data_gateway.CSV_HALGateway.read_value","title":"read_value","text":"<pre><code>read_value(index_: Optional[int] = None, input_id: Union[str, int] = 'Impedancia') -&gt; float\n</code></pre> <p>Read a value from the simulated CSV data.</p> <p>Parameters:</p> Name Type Description Default <code>index_</code> <code>Optional[int]</code> <p>The row index to read from. If None, reads the next row.</p> <code>None</code> <code>input_id</code> <code>str | int</code> <p>The column name or index to read from.</p> <code>'Impedancia'</code> <p>Returns:</p> Name Type Description <code>float</code> <code>float</code> <p>The value read from the CSV data.</p> <p>Raises:</p> Type Description <code>AssertionError</code> <p>If input types are incorrect.</p> <code>IndexError</code> <p>If the index is out of bounds.</p> Source code in <code>src/nanotherm/infrastructure/gateways/simul_data_gateway.py</code> <pre><code>def read_value(\n    self, \n    index_: Optional[int] = None, \n    input_id: Union[str, int] = 'Impedancia'\n) -&gt; float:\n    \"\"\"\n    Read a value from the simulated CSV data.\n\n    Args:\n        index_ (Optional[int]): The row index to read from. If None, reads the next row.\n        input_id (str | int): The column name or index to read from.\n\n    Returns:\n        float: The value read from the CSV data.\n\n    Raises:\n        AssertionError: If input types are incorrect.\n        IndexError: If the index is out of bounds.\n    \"\"\"\n    assert index_ is None or isinstance(index_, int), \"index_ should be of type 'int' or None\"\n    assert isinstance(input_id, (str, int)), \"input_id should be of type 'str' or 'int'\"\n\n    if self.__index == self._data_len-1:\n        log.info('No more data to be read from CSV file.')\n        log.debug(f'Final index read from {self.__path}: {self.__index}.')\n        self.__reach_end = True\n\n    if index_ is None:\n        index = self.__index\n        self.__index += 1\n    else:\n        index = index_\n\n    if index &gt;= self._data_len:\n        log.error(f'Index value [ {index} ] out of data bounds. Data length is {self._data_len}')\n        raise IndexError(f'Index {index} out of bounds for data length {self._data_len}')\n\n    current_row = self._simul_data.iloc[index]\n    value = current_row[input_id] \n    #log.debug(f'Value read from index {index}: {value}')\n\n    return float(value)\n</code></pre>"},{"location":"reference/core/infrastructure/simulation_gateway/#nanotherm.infrastructure.gateways.simul_data_gateway.CSV_HALGateway.write_value","title":"write_value","text":"<pre><code>write_value(output_id: str, value: float) -&gt; None\n</code></pre> <p>Simulate writing a value to a hardware output.</p> <p>Parameters:</p> Name Type Description Default <code>output_id</code> <code>str</code> <p>The identifier of the output to write to.</p> required <code>value</code> <code>float</code> <p>The value to write.</p> required <p>Returns:</p> Type Description <code>None</code> <p>None</p> Source code in <code>src/nanotherm/infrastructure/gateways/simul_data_gateway.py</code> <pre><code>def write_value(self, output_id: str, value: float) -&gt; None:\n    \"\"\"\n    Simulate writing a value to a hardware output.\n\n    Args:\n        output_id (str): The identifier of the output to write to.\n        value (float): The value to write.\n\n    Returns:\n        None\n    \"\"\"\n    log.info(f\"Simulated writing {value} to: {output_id}\")\n</code></pre>"},{"location":"reference/core/infrastructure/simulation_gateway/#classes","title":"Classes","text":"<ul> <li><code>CSV_HALGateway</code>: Simulated HAL gateway using CSV files for input data.</li> </ul> <p>Imports: - Input Gateway</p>"},{"location":"reference/core/services/control_loop/","title":"Control Loop Service","text":"<p>This module implements the main control loop with interrupt-like timing for the Nanotherm system.</p>"},{"location":"reference/core/services/control_loop/#code-reference","title":"Code Reference","text":""},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop","title":"control_loop","text":"<p>Control loop service implementation with interrupt-like timing.</p>"},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop.ControlLoop","title":"ControlLoop","text":"<p>Main control loop service.</p> <p>Handles the execution of the control loop with interrupt-like timing, measurement acquisition, and actuation.</p> <p>Attributes:</p> Name Type Description <code>controller</code> <code>PIDController</code> <p>The PID controller instance.</p> <code>sample_time</code> <code>float</code> <p>The control loop sample time in seconds.</p> <code>hal</code> <code>I_HALGateway</code> <p>Hardware abstraction layer gateway for measurements.</p> <code>_simulation</code> <code>bool</code> <p>Whether the loop is running in simulation mode.</p> <code>_simulation_data</code> <code>List[Dict[str, Any]]</code> <p>Collected simulation data.</p> Source code in <code>src/nanotherm/services/control_loop.py</code> <pre><code>class ControlLoop:\n    \"\"\"\n    Main control loop service.\n\n    Handles the execution of the control loop with interrupt-like timing,\n    measurement acquisition, and actuation.\n\n    Attributes:\n        controller (PIDController): The PID controller instance.\n        sample_time (float): The control loop sample time in seconds.\n        hal (I_HALGateway): Hardware abstraction layer gateway for measurements.\n        _simulation (bool): Whether the loop is running in simulation mode.\n        _simulation_data (List[Dict[str, Any]]): Collected simulation data.\n    \"\"\"\n\n    def __init__(\n        self, \n        controller: PIDController, \n        sample_time: float, \n        gateway: I_HALGateway, \n        simulating: bool = False\n    ) -&gt; None:\n        \"\"\"\n        Initialize the control loop.\n\n        Args:\n            controller (PIDController): The PID controller instance.\n            sample_time (float): The control loop sample time in seconds.\n            gateway (I_HALGateway): Hardware abstraction layer gateway.\n            simulating (bool, optional): Whether to run in simulation mode. Defaults to False.\n        \"\"\"\n        self.controller = controller\n        self.sample_time = sample_time\n        self.hal = gateway\n        self._stop_event = threading.Event()\n        self._timer_thread = None  # Timer thread that directly calls _run()\n        self._simulation = simulating\n        self._start_time = 0.0\n        self._measurement = 0.0\n        self._simulation_data = []  # Store simulation results\n\n    def start(self) -&gt; None:\n        \"\"\"\n        Start the control loop execution with interrupt-like timing.\n        Spawns a background thread to periodically execute the control logic.\n        \"\"\"\n        if self._timer_thread is None or not self._timer_thread.is_alive():\n\n            self._stop_event.clear()\n            self._start_time = time.monotonic() if not self._simulation else 0.0\n            self._measurement = 0.0\n            self._timestamp = 0.0\n\n            # Start the timer thread that directly calls _run()\n            self._timer_thread = threading.Thread(\n                target=self._timer_worker,\n                name='ControlTimerThread',\n                daemon=True\n            )\n            self._timer_thread.start()\n\n            log.info(f'Control loop started with {self.sample_time*1000:.1f}ms interrupt interval')\n\n    def stop(self) -&gt; None:\n        \"\"\"\n        Stop the control loop execution and wait for the thread to finish.\n        \"\"\"\n        self._stop_event.set()\n\n        if self._timer_thread is not None:\n            self._timer_thread.join(timeout=1.0)\n            self._timer_thread = None\n\n        log.info('Control loop terminated.')\n\n    def _internal_stop(self) -&gt; None:\n        \"\"\"\n        Internal method to stop the control loop.\n        \"\"\"\n        self._stop_event.set()\n        log.info('Control loop stopped internally.')\n\n    def thread_is_alive(self) -&gt; bool:\n        \"\"\"\n        Check if the control loop thread is still running.\n\n        Returns:\n            bool: True if the control loop thread is alive, False otherwise.\n        \"\"\"\n        return self._timer_thread is not None and self._timer_thread.is_alive()\n\n    def wait_for_completion(self) -&gt; None:\n        \"\"\"\n        Wait for the control loop thread to finish execution.\n        \"\"\"\n        if self._timer_thread is not None:\n            self._timer_thread.join()\n\n    def get_simulation_data(self) -&gt; List[Dict[str, Any]]:\n        \"\"\"\n        Get the collected simulation data.\n\n        Returns:\n            List[Dict[str, Any]]: A copy of the simulation data collected during the run.\n        \"\"\"\n        return self._simulation_data.copy()\n\n    def clear_simulation_data(self) -&gt; None:\n        \"\"\"\n        Clear the simulation data.\n        \"\"\"\n        self._simulation_data.clear()\n\n    def _timer_worker(self) -&gt; None:\n        \"\"\"\n        Timer thread that periodically calls the control loop directly.\n        Handles timing and stop event checking.\n        \"\"\"\n        log.info(f\"Timer thread started: {threading.current_thread().name}\")\n\n        next_time = time.monotonic()\n\n        while not self._stop_event.is_set():\n            next_time += self.sample_time\n\n            # Sleep until next execution time\n            sleep_time = next_time - time.monotonic()\n            if sleep_time &gt; 0:\n                if self._stop_event.wait(timeout=sleep_time):\n                    break  # Stop event was set during sleep\n\n            if not self._stop_event.is_set():\n                self._run()\n\n        log.info(\"Timer thread stopped\")\n\n    def _run(self) -&gt; None:\n        \"\"\"\n        Main control loop execution - called on each 'interrupt'.\n        Computes control action, reads measurements, logs data, and checks for simulation end.\n        \"\"\"\n        if self._stop_event.is_set():\n            return\n\n        try:\n            # Compute control action\n            timestamp = time.monotonic() - self._start_time\n            if self._simulation:\n                self._timestamp += self.controller.Ts\n            else:\n                self._timestamp = timestamp\n            control_action = self.controller.compute(self._measurement, self._timestamp)\n            resistance = self.hal.read_value()\n            self._measurement = control_action**2 / resistance\n\n            # Log simulation data if simulating\n            if self._simulation:\n                setpoint = self.controller.setpoint\n\n                self._simulation_data.append({\n                    'timestamp': self._timestamp,\n                    'resistence': resistance,\n                    'control_action': control_action,\n                    'setpoint': setpoint,\n                    'measurement': self._measurement,\n                })\n\n            # Check simulation end\n            if self._simulation and getattr(self.hal, 'reach_end', False):\n                log.info('End of simulated data reached. Stopping control loop.')\n                self._internal_stop()\n                return\n\n        except Exception as e:\n            log.error(f\"Error in control loop: {e}\")\n            self._internal_stop()\n            return\n</code></pre>"},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop.ControlLoop.__init__","title":"__init__","text":"<pre><code>__init__(controller: PIDController, sample_time: float, gateway: I_HALGateway, simulating: bool = False) -&gt; None\n</code></pre> <p>Initialize the control loop.</p> <p>Parameters:</p> Name Type Description Default <code>controller</code> <code>PIDController</code> <p>The PID controller instance.</p> required <code>sample_time</code> <code>float</code> <p>The control loop sample time in seconds.</p> required <code>gateway</code> <code>I_HALGateway</code> <p>Hardware abstraction layer gateway.</p> required <code>simulating</code> <code>bool</code> <p>Whether to run in simulation mode. Defaults to False.</p> <code>False</code> Source code in <code>src/nanotherm/services/control_loop.py</code> <pre><code>def __init__(\n    self, \n    controller: PIDController, \n    sample_time: float, \n    gateway: I_HALGateway, \n    simulating: bool = False\n) -&gt; None:\n    \"\"\"\n    Initialize the control loop.\n\n    Args:\n        controller (PIDController): The PID controller instance.\n        sample_time (float): The control loop sample time in seconds.\n        gateway (I_HALGateway): Hardware abstraction layer gateway.\n        simulating (bool, optional): Whether to run in simulation mode. Defaults to False.\n    \"\"\"\n    self.controller = controller\n    self.sample_time = sample_time\n    self.hal = gateway\n    self._stop_event = threading.Event()\n    self._timer_thread = None  # Timer thread that directly calls _run()\n    self._simulation = simulating\n    self._start_time = 0.0\n    self._measurement = 0.0\n    self._simulation_data = []  # Store simulation results\n</code></pre>"},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop.ControlLoop.start","title":"start","text":"<pre><code>start() -&gt; None\n</code></pre> <p>Start the control loop execution with interrupt-like timing. Spawns a background thread to periodically execute the control logic.</p> Source code in <code>src/nanotherm/services/control_loop.py</code> <pre><code>def start(self) -&gt; None:\n    \"\"\"\n    Start the control loop execution with interrupt-like timing.\n    Spawns a background thread to periodically execute the control logic.\n    \"\"\"\n    if self._timer_thread is None or not self._timer_thread.is_alive():\n\n        self._stop_event.clear()\n        self._start_time = time.monotonic() if not self._simulation else 0.0\n        self._measurement = 0.0\n        self._timestamp = 0.0\n\n        # Start the timer thread that directly calls _run()\n        self._timer_thread = threading.Thread(\n            target=self._timer_worker,\n            name='ControlTimerThread',\n            daemon=True\n        )\n        self._timer_thread.start()\n\n        log.info(f'Control loop started with {self.sample_time*1000:.1f}ms interrupt interval')\n</code></pre>"},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop.ControlLoop.stop","title":"stop","text":"<pre><code>stop() -&gt; None\n</code></pre> <p>Stop the control loop execution and wait for the thread to finish.</p> Source code in <code>src/nanotherm/services/control_loop.py</code> <pre><code>def stop(self) -&gt; None:\n    \"\"\"\n    Stop the control loop execution and wait for the thread to finish.\n    \"\"\"\n    self._stop_event.set()\n\n    if self._timer_thread is not None:\n        self._timer_thread.join(timeout=1.0)\n        self._timer_thread = None\n\n    log.info('Control loop terminated.')\n</code></pre>"},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop.ControlLoop.thread_is_alive","title":"thread_is_alive","text":"<pre><code>thread_is_alive() -&gt; bool\n</code></pre> <p>Check if the control loop thread is still running.</p> <p>Returns:</p> Name Type Description <code>bool</code> <code>bool</code> <p>True if the control loop thread is alive, False otherwise.</p> Source code in <code>src/nanotherm/services/control_loop.py</code> <pre><code>def thread_is_alive(self) -&gt; bool:\n    \"\"\"\n    Check if the control loop thread is still running.\n\n    Returns:\n        bool: True if the control loop thread is alive, False otherwise.\n    \"\"\"\n    return self._timer_thread is not None and self._timer_thread.is_alive()\n</code></pre>"},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop.ControlLoop.wait_for_completion","title":"wait_for_completion","text":"<pre><code>wait_for_completion() -&gt; None\n</code></pre> <p>Wait for the control loop thread to finish execution.</p> Source code in <code>src/nanotherm/services/control_loop.py</code> <pre><code>def wait_for_completion(self) -&gt; None:\n    \"\"\"\n    Wait for the control loop thread to finish execution.\n    \"\"\"\n    if self._timer_thread is not None:\n        self._timer_thread.join()\n</code></pre>"},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop.ControlLoop.get_simulation_data","title":"get_simulation_data","text":"<pre><code>get_simulation_data() -&gt; List[Dict[str, Any]]\n</code></pre> <p>Get the collected simulation data.</p> <p>Returns:</p> Type Description <code>List[Dict[str, Any]]</code> <p>List[Dict[str, Any]]: A copy of the simulation data collected during the run.</p> Source code in <code>src/nanotherm/services/control_loop.py</code> <pre><code>def get_simulation_data(self) -&gt; List[Dict[str, Any]]:\n    \"\"\"\n    Get the collected simulation data.\n\n    Returns:\n        List[Dict[str, Any]]: A copy of the simulation data collected during the run.\n    \"\"\"\n    return self._simulation_data.copy()\n</code></pre>"},{"location":"reference/core/services/control_loop/#nanotherm.services.control_loop.ControlLoop.clear_simulation_data","title":"clear_simulation_data","text":"<pre><code>clear_simulation_data() -&gt; None\n</code></pre> <p>Clear the simulation data.</p> Source code in <code>src/nanotherm/services/control_loop.py</code> <pre><code>def clear_simulation_data(self) -&gt; None:\n    \"\"\"\n    Clear the simulation data.\n    \"\"\"\n    self._simulation_data.clear()\n</code></pre>"},{"location":"reference/core/services/control_loop/#classes","title":"Classes","text":"<ul> <li><code>ControlLoop</code>: Main control loop service for measurement, actuation, and timing.</li> </ul> <p>Imports: - PID Controller - Input Gateway</p>"}]}